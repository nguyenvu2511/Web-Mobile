@model IEnumerable<Web_Mobile.Models.LoaiSanPham>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3>QUẢN LÝ LOẠI SẢN PHẨM</h3>

<p>
    @Html.ActionLink("Thêm Mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Loại SP
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLSP)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.MaLSP }) |
            @Html.ActionLink("Chi Tiết", "Details", new { id=item.MaLSP }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.MaLSP })
        </td>
    </tr>
}

</table>
